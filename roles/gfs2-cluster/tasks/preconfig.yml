---
- hosts: all
  become: true
  become_user: root
  tasks:
  - name: "Изменяем файл /etc/hosts"
    copy:
      src: ../files/hosts
      dest: /etc/hosts
      owner: root
      group: root
      mode: '0644'
      backup: yes 
  
